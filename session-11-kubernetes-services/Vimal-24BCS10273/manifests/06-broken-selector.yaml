apiVersion: v1
kind: Service
metadata:
  name: campus-broken-service
spec:
  type: ClusterIP
  # Deliberately wrong: the running Pods are labelled app=campus-web.
  selector:
    app: campus-web-backend
  ports:
    - port: 80
      targetPort: 80
